<%= form_for(@todo) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :completed %>
  <%= f.check_box :complete %>
  <%= f.submit %>
<% end %>

<%= link_to 'Home', to_dos_path %>